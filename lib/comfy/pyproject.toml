[project]
name = "comfy"
version = "0.1.0"
dependencies = [
    "httpx>=0.28.1",
    "huggingface-hub>=0.27.0",
    "boto3>=1.34.0",
    "runpod>=1.7.0",
]

[tool.setuptools]
packages = ["comfy"]

[tool.setuptools.package-dir]
"comfy" = "."
