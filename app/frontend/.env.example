# GraphQL API Endpoint
PUBLIC_GRAPHQL_ENDPOINT=http://localhost:4002/api/graphql

# Mock Mode (set to 'true' to use mock data instead of GraphQL)
PUBLIC_GRAPHQL_MOCK=true

# Gemini API Key (for Bunny AI features)
VITE_GEMINI_API_KEY=
